<Window x:Class="ReatsTracker.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReatsTracker.Desktop"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#526D82"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#435a6b"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.ColumnSpan="3" Background="#dde6ed"
        BorderBrush="#526d82" BorderThickness="0.5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Margin="10,0">
                    <Button Content="Add company"
                    Style="{StaticResource RoundedButtonStyle}"
                    Width="110" Height="30"
                    Margin="0,0,10,0"/>
                    <Button Content="Refresh"
                    Style="{StaticResource RoundedButtonStyle}"
                    Width="110" Height="30"/>
                </StackPanel>

                <TextBlock Grid.Column="2"
                   Text="reats-app"
                   FontSize="18"
                   Foreground="#526d82"
                   FontWeight="ExtraLight"
                   VerticalAlignment="Center"
                   Margin="0,0,20,3"/>
            </Grid>
        </Border>


        <Border Grid.Row="1"
                Grid.Column="0"
                Margin="10"
                Background="White"
                BorderBrush="#526d82"
                BorderThickness="0.5"
                CornerRadius="5">

            <Grid>
                <Border x:Name="ClipMask" Background="White" CornerRadius="4.5"/>

                <ListBox 
                    x:Name="CompaniesList" 
                    ItemsSource="{Binding Companies}" 
                    DisplayMemberPath="Name"
                    Background="Transparent"
                    BorderThickness="0"
                    HorizontalContentAlignment="Stretch"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                    <ListBox.OpacityMask>
                        <VisualBrush Visual="{Binding ElementName=ClipMask}"/>
                    </ListBox.OpacityMask>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="Foreground" Value="#526d82"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="ItemBorder"
                                                Background="Transparent"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>

                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#dde6ed"/>
                                                <Setter Property="Foreground" Value="Black"/>
                                            </Trigger>

                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#526d82"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>

                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>


        <Border Grid.Row="1" 
                Grid.Column="1" 
                Grid.ColumnSpan="2" 
                Margin="0,10,10,10" 
                Background="White" 
                BorderBrush="#526d82"
                BorderThickness="0.5" 
                CornerRadius="5">
            <TextBlock Text="Select a company on the left to view its vacancies..." 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       Foreground="Gray" 
                       FontSize="14"
                       FontWeight="Light"/>
        </Border>
    </Grid>
</Window>
